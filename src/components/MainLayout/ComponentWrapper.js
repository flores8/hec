import styled from "styled-components";
import { width, spacing } from "../../utils";

export const NonFullWidth = styled.div`
  margin: auto;
  max-inline-size: ${width.xxlg};
  padding-inline-start: ${spacing.s5};
  padding-inline-end: ${spacing.s5};
`;
